[package]
name = "busd-server"
description = "Generic local message bus and IPC broker."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
keywords.workspace = true
categories.workspace = true

[[bin]]
name = "busd"
path = "src/main.rs"

[features]
default = ["unix"]
unix = ["dep:bus-transport-unix"]
dbus = ["dep:bus-transport-dbus"]
monitoring = []

[dependencies]
bus-broker = { path = "../bus-broker", version = "0.1.0" }
busd-client = { package = "busd-client", path = "../bus-client", version = "0.1.0" }
bus-policy = { path = "../bus-policy", version = "0.1.0" }
bus-protocol = { path = "../bus-protocol", version = "0.1.0" }
bus-transport-unix = { path = "../bus-transport-unix", version = "0.1.0", optional = true }
bus-transport-dbus = { path = "../bus-transport-dbus", version = "0.1.0", optional = true }

[dev-dependencies]
busd-client = { package = "busd-client", path = "../bus-client", version = "0.1.0" }

[lints]
workspace = true
